import { useFormContext } from "react-hook-form"

const FormContextComponent = () => {
    // const { getValues } = useFormContext()
    // console.log("CONTEXT", getValues("firstName"))
    return (
        <div></div>
    )
}

export default FormContextComponent